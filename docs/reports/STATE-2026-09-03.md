# STATE 2026-09-03, EXECUTOR

Read at **2026-09-03T19:48Z to 20:05Z**. Every number below was read from the
repository, from the GitHub API, or from the live production health route during
this run. Nothing here is carried over from a previous session's report or from a
dispatch, and where a fact could not be read, this file says so instead of
supplying one.

---

## 1. `origin/main`, and every open pull request

**`origin/main` = `31a817d4cfc9a1d93e13eea5a63dde9dddccbb39`**
(`BOARD: EXT-14 se inchide ca DROP, si trei carduri noi din rezultatul scanarii
lui Andre (#174)`, merged 2026-09-03T17:45Z).

Five pull requests are open. The last column is the one that matters and it is
read from `repos/:owner/:repo/commits/<head sha>/check-runs`, not from
`gh pr checks`, because that summary reports a result without saying which commit
produced it.

| PR | title | head branch | base | mergeable / state | head sha | `quality` run on THAT sha |
|---|---|---|---|---|---|---|
| #177 | EXT-15: document_source, and what an unread scan shows | `card/ext-15` | **`board/andre-reconciliation`** | MERGEABLE / CLEAN | `eaa1557` | **yes**, success, 17m54s |
| #176 | BOARD: EXT-16 unblocked with Andre's tolerance, and EXT-18..EXT-20 | `board/andre-reconciliation` | `main` | MERGEABLE / **BLOCKED** | `678e31e` | **NO RUN EXISTS** |
| #175 | EXT-14: confidence removed from the payload and the contract | `card/ext-14` | `main` | MERGEABLE / CLEAN | `56e1125` | **yes**, success, 16m33s |
| #172 | TRIAGE 20260903-070005: one allocator and not two, plus an apply nobody owns | `triage/20260903-070005` | `main` | MERGEABLE / CLEAN | `a62f0ea` | **yes**, success, 15m47s |
| #157 | TRIAGE 20260902-070904: R-090 to R-095, three ruling ids stranded on a conflicting PR | `triage/20260902-070904` | `main` | MERGEABLE / CLEAN | `e7b663b` | **yes**, success, 16m11s |

### The one that is not green, stated precisely

**PR #176 has no `quality` run of any kind against its head sha `678e31e`.** Not
pending, not failed, not skipped: absent. The only `quality` run that has ever
existed on `board/andre-reconciliation` is against `7706def`, an earlier commit,
at 2026-09-03T16:07:53Z. The commit now at the head, `678e31e`
(`BOARD: resolve rc-board-phase3.json against main BY CARD IDENTITY, and a defect
in the first attempt`), was pushed at 2026-09-03T17:49:52Z and created no run.
Runs for four other branches were created at 17:46, 17:47, 17:48 and 17:50, so
the Actions queue was working either side of it. `mergeStateStatus: BLOCKED`
beside `mergeable: MERGEABLE` is the required check being absent rather than red.

**Why this is the load-bearing fact of the whole report:** #177 does not target
`main`. Its base is `board/andre-reconciliation`, so the two are a stack, and
**both EXT-15 and the EXT-16 unblock are behind that one missing run.** The cause
was not determined and is not guessed at here.

### Two shipped cards carry `#PENDING` where a pull request number belongs

Not asked for, found while reading item 7, reported and not acted on. `P3-11a`,
`P3-11b` and `P3-11e` are all `status: shipped` with `evidence.ref` beginning
`#PENDING`. Section 6 requires a ref that lets a stranger re-verify without
asking anyone. The real numbers exist: P3-11a is #161, P3-11b is #162.

---

## 2. Board counts, and every card carrying a `blocked_on`

Counted from `origin/main`.

| board | total | shipped | todo | in_flight | blocked | launch gate |
|---|---|---|---|---|---|---|
| `rc-board.json` (phase 1) | 13 | 13 | 0 | 0 | 0 | closed |
| `rc-board-phase2.json` | 62 | 43 | 16 | 1 | 2 | **6 / 9** |
| `rc-board-phase3.json` | 62 | 29 | 32 | 0 | 1 | **0 / 9** |

**Every card with `blocked_on` set: three, across all three boards.**

| card | board | status | `blocked_on` |
|---|---|---|---|
| P2-08b | phase 2 | blocked | `andre` |
| P2-14 | phase 2 | blocked | `client` |
| EXT-16 | phase 3 | blocked | `andre` |

Launch gate conditions carrying a `blocked_on`: **G7 on phase 2, on `ivan`.** No
phase 3 gate condition carries one, and all nine of them are `fail`.

**On the branch, not on `main`:** PR #176's version of the phase 3 board has 65
cards, 29 shipped and 36 todo, **zero blocked**, because it is the pull request
that clears EXT-16 and adds EXT-18 to EXT-20. None of that is on `main`.

---

## 3. EXT-16

**Status on `origin/main`: `blocked`. `blocked_on: "andre"`. `evidence: null`.**
`depends_on: ["EXT-15"]`.

`evidence` is asked for verbatim, so: the field is JSON `null`, not a string, not
an empty object. There is nothing in it to quote.

### Is each acceptance line a runnable command with an expected exit code

The acceptance on `main` is one paragraph. Split at its own `PLUS` markers, it is
five clauses, and **two of the five are runnable commands with an exit code**:

| # | clause | runnable command? | exit code named? |
|---|---|---|---|
| 1 | `npx playwright test tests/e2e/extraction.spec.ts` exits 0, with three named cases | **yes** | **yes, 0** |
| 2 | "Each must fail before the change and the PR must show both results" | no, a procedure over the PR | no |
| 3 | "the tolerance is a single named constant, read from one place, and `docs/contracts/extraction-v2.md` records it as the AGREED value with Andre named as its source" | **no**, prose, checkable only by reading | no |
| 4 | "the check runs on the callback route ... proven by a case that posts directly to the route" | no independent command; it is a constraint on a case inside clause 1 | no |
| 5 | `npx tsc --noEmit` exits 0 | **yes** | **yes, 0** |

**So: 2 of 5 are commands with an expected exit code. Clause 3 is the one that
cannot be checked by running anything**, and it is the clause carrying the
requirement the card exists for, that there is exactly one tolerance and one
source of it.

### The version on PR #176 is different and is stronger, and it is not merged

The branch version adds a sixth clause, `PLUS COMMAND: a named check exits 0 over
the tolerance arithmetic itself`, with three fixture values (a 7-line document
tolerating 0.07, a 54-line document tolerating 0.54, a 3-line document tolerating
0.05 where the floor wins). **It names an exit code but does not name the
command**, so it is a command-shaped requirement whose command the implementer
still has to choose. The branch version also replaces "the agreed tolerance" with
Andre's formula written out:
`abs(sum_of_line_totals - target) <= max(0.05, 0.01 * line_count)`, both sides
rounded to two decimals before comparing.

---

## 4. Applied ledger in production against the migration files in the repository

**These two sources disagree, and the disagreement is the finding.**

- **Production says `0031`.** `GET https://rc-inventory-iota.vercel.app/api/health`
  returned `{"commit":"31a817d4cfc9a1d93e13eea5a63dde9dddccbb39","ledger_version":"0031","at":"2026-09-03T19:49:08.392Z"}`,
  HTTP 200. That number is `max(version)` from `supabase_migrations.schema_migrations`,
  read through `public.applied_ledger_version()`.
- **The highest migration file on `main` is `0031_units_tonne_litre_rows.sql`.**
  31 files total.
- **`docs/migrations/APPLY-LOG.md` on `main` declares four of them still pending**,
  in its machine-read pending register:
  - `0028_applied_ledger_version.sql`, card de aplicare P3-11e
  - `0029_category_paints.sql`, card de aplicare P3-34
  - `0030_units_tonne_litre.sql`, card de aplicare P3-33
  - `0031_units_tonne_litre_rows.sql`, card de aplicare P3-33
- **The last APPLY-LOG entry of any kind is `0027_drop_products_supplier_name.sql - APPLIED`**,
  and its own post-check records `ledger -> 27 rows, highest 0027`. No entry
  exists for 0028, 0029, 0030 or 0031, on `main` or on any of the 48 remote
  branches: a search of `docs/migrations/APPLY-LOG.md` on every branch for
  `0031_units_tonne_litre_rows.sql - APPLIED` returned nothing.

### What can be proven from that, and what cannot

**Proven: `0028` is applied in production.** `applied_ledger_version()` is the
function that `0028` creates. The route returned a string rather than `null`, and
`null` is what the route returns when the RPC errors, so the function exists and
is callable by `service_role`.

**Proven: the ledger's highest version is `0031`.** That is the value returned.

**Not proven, because a per-row read of the ledger was not obtained: whether
`0029` and `0030` have their own rows.** `max()` alone cannot separate "all four
applied" from "0028 and 0031 applied, two skipped". No per-row read was performed
because this machine has **no `psql` binary and no `pg` driver in
`node_modules`**; the repository's applier shells out to `psql` inside a Docker
container and is hard-coded to a specific historical 13-file batch, so it is not
a general reader. **No production credential was read during this run.**

### So the answer to "name every merged-but-unapplied migration file"

**By the repository's own register: `0028`, `0029`, `0030`, `0031`.**
**By production: at most `0029` and `0030`, and possibly none.**
The repository's register is stale by at least one file and, on the most likely
reading, by all four.

### The context that makes this worth reading twice

- **`Supabase Preview` ran on `main` sha `31a817d` and concluded `success` at
  2026-09-03T17:45:51Z**, alongside `quality`. That is a Supabase GitHub
  integration check, and it is the only production-adjacent thing that ran on
  that push. **A write path that reaches production without a row in either
  journal is exactly what CLAUDE.md 8.8 forbids**, whichever way this resolves.
  This report does not assert that the integration performed the apply. It
  asserts that production's ledger moved to 0031, that no journal entry exists
  for it, and that no card claims to have run it.
- **TRIAGE PR #172 already found the near half of this, as ruling R-089**, and
  authored card **P3-35** for it: `APPLY-LOG.md` names P3-11e as the apply card
  for `0028`, and P3-11e is `shipped`, so no open card carries the apply. R-089
  concludes the apply is unowned. **This report goes further: it is not unowned,
  it appears to have already happened.**
- **PR #177 is written against the stale register.** Its body says `0032` is
  "pending alongside `0028`-`0031`". If those are applied, `0032` will be applied
  by the same unjournalled path the moment #176 and #177 merge.

---

## 5. Deployed sha against `origin/main` sha

Both values, not interpreted, as asked.

| | value |
|---|---|
| `origin/main` | `31a817d4cfc9a1d93e13eea5a63dde9dddccbb39` |
| `/api/health` `commit` | `31a817d4cfc9a1d93e13eea5a63dde9dddccbb39` |

Read at 2026-09-03T19:49:08Z, HTTP 200, `cache-control: no-store`.

---

## 6. The committed ruling counter

**`decisions/NEXT-RULING-ID` on `origin/main` contains `R-087`**, one line, nothing
else.

The highest ruling id actually written into `decisions/inbox.md` on `main` is
**`R-086`**, so the counter is ahead of the highest written by exactly one, which
is what `npm run check:unique-ids` requires.

**Both open TRIAGE pull requests allocate from `R-087`.** #172 states in its body
that it takes R-087 to R-091 and advances the counter to `R-092`. #157 is titled
`R-090 to R-095`. Neither is merged, so `main` is consistent today; the two of
them are not consistent with each other, and whichever merges second will need a
resolution rather than a merge. That is the collision CLAUDE.md 8b was written to
turn into a conflict, and here it is doing so.

---

## 7. P3-11a

**Landed on `main`. Yes.**

**Commit `4b2b85305829cdf08bf1d1741f1f4b6e97d74180`** -
`P3-11a: rulings trec printr-un spool, deci un singur proces citeste botul (#161)`.
Verified as an ancestor of `origin/main` with `git merge-base --is-ancestor`,
which exited 0.

The card is `status: shipped` on the phase 3 board. Its `evidence.ref` begins
`#PENDING` rather than `#161`, per item 1.

---

## 8. `scripts/poc/ask.sh`

| question | answer |
|---|---|
| present on `main`? | **yes**, `git ls-tree origin/main scripts/poc/ask.sh` returns a blob |
| executable? | **yes**, mode `100755` in the tree, and `-rwxr-xr-x` in the working copy |
| deadline uses `date +%s` rather than `sleep`? | **yes** |

The deadline is computed once, at line 197:

    ASK_DEADLINE=$(( $(date +%s) + ASK_DEADLINE_SECONDS ))

and the wait loop tests the clock against it, at line 109:

    if [ "$(date +%s)" -ge "$WFA_DEADLINE" ]; then

`sleep` appears in that loop exactly once, at line 112, as `sleep "$ASK_POLL_SECONDS"`,
which is the **poll interval and not the deadline**. The file says so itself at
lines 97 to 101: a long sleep across a suspend costs one late poll, where a long
sleep AS the deadline costs the deadline itself. This is the shape CLAUDE.md 14
requires and it is implemented as written.

---

## Step 2. EXT-16 is not claimable, and here is exactly why

**Three of the eligibility conditions in CLAUDE.md section 2 are tested and EXT-16
fails two of them on `main` today.**

| condition | EXT-16 on `origin/main` | met? |
|---|---|---|
| `status` is `todo` | `blocked` | **no** |
| `blocked_on` is `null` | `"andre"` | **no** |
| every `depends_on` is `shipped` | `["EXT-15"]`, and EXT-15 is `todo` | **no** |

**Merging PR #176 alone would not make it claimable.** #176 fixes the first two:
on that branch EXT-16 is `todo` with `blocked_on: null`. It does not fix the
third. EXT-15 ships in **#177**, which is stacked on #176 and cannot merge before
it.

**So the chain is: #176 needs a `quality` run that does not exist, then #176
merges, then #177 retargets and merges, then EXT-15 is `shipped`, and only then is
EXT-16 claimable.** The first link is a missing CI run, which is not a decision
anybody owes an answer to and is therefore not an `ask.sh` item.

Per the dispatch, the next eligible card in board order is claimed instead.

### The card claimed: EXT-09

Lowest-id eligible card on the phase 3 board that no open pull request already
carries. EXT-14 and EXT-15 are lower but are #175 and #177. EXT-09 has
`status: todo`, `blocked_on: null`, `depends_on: []`, `owner_terminal: executor`,
`gate: green_self_merge`, and no branch exists for it.

**Title:** Drop `_meta.characters_extracted` from the extraction contract and put a
model-reported page count in its place.

**Claims read from `docs/poc/state.json` on `main` before claiming:** one entry,
`AUT-15`, `claimed_by: harness`, `claimed_at: 2026-09-03T12:15:44Z`. That lease
expired at 18:15:44Z and is six hours plus ninety minutes old at the time of this
read. It does not touch EXT-09 either way.

---

## Findings carried forward, none of which blocked this run

1. **PR #176 has no `quality` run on its head sha**, and the EXT-15 / EXT-16 stack
   is behind it. Item 1.
2. **Production's ledger is at 0031 while the repository's pending register says
   0028 to 0031 are unapplied**, with no APPLY-LOG entry for any of them and no
   card claiming the apply. Item 4. R-089 and card P3-35 already cover the
   ownership half; the apply-appears-to-have-happened half is new here.
3. **Three shipped cards carry `#PENDING` in `evidence.ref`.** Item 1.
4. **The two open TRIAGE pull requests allocate overlapping ruling ids** from the
   same counter value. Item 6.
