# EXECUTOR run 20260908-070003

**Role:** EXECUTOR, unattended scheduled run, CLAUDE.md section 13
**Date:** 2026-09-08
**Worktree:** `/Users/ivan/rc-inventory-poc-run`, detached at `origin/main` `49fef9a`
**Cap:** 45 minutes

---

## Boot status report, as printed before any write

| board | shipped | in_flight | todo | blocked | halted |
|---|---|---|---|---|---|
| `rc-board-phase2.json` | 65 | 1 | 16 | 3 | 0 |
| `rc-board-phase3.json` | 48 | 0 | 25 | 0 | 0 |
| total | 113 | 1 | 41 | 3 | 0 |

Launch gates at boot: **phase 2 = 6/9** (G4, G7, G9 fail). **Phase 3 = 0/9.**

Blocked: P2-08b on `andre`, P2-14 on `client`, MIG-01 on `ivan`. In flight at
boot: AUT-3, on the phase 2 board, untouched by this run.

Claims held at boot: `EXT-11` and `GATE-01`, both by `harness`, both on cards
already shipped. Nothing eligible was claimed by another actor, so nothing was
skipped for that reason.

**Next eligible card: GATE-02.** 39 eligible in total.

---

## Cards touched

### GATE-02, shipped

**"Re-run the phase 3 gate audit: every one of the nine conditions is recorded
against a premise that P3-27 discharged."**

Claimed, flipped to `in_flight` in its own commit before any work, worked,
shipped, claim file deleted.

**Result: the phase 3 gate stays at 0 of 9, counted, not estimated.** The card's
defaults say plainly that the count may go up, down or nowhere and that this card
may not flip a condition to move it. It did not move.

**What DID change is every reason.** Not one of the nine still fails for the
blocker recorded against it on 2026-08-31, which was the same sentence nine
times: "no phase 3 migration has been applied to the RC Supabase project".
Production reports `ledger_version 0036` read from its own database. That premise
is gone and the audit replaced it condition by condition:

- **G1** is 3 of 4 clauses MET. The one gap is an unauthenticated READ, which no
  committed command performs. One card away from a pass, and it is the cheapest
  point left on the board.
- **G2**'s instrument, `public.unassigned_outbound_count()`, is live on
  production and nothing has ever read it there.
- **G3, G4, G5 and G6** are all held by one missing capability: an authenticated
  verification against the deployed screens. The old audit said such a check
  "would find an error page". It would not any more. It has simply never run.
- **G6 moved most.** Its evidence read "EVIDENCE FOUND: none. P3-12 is todo".
  P3-12 and the whole P3-13 chain have shipped since.
- **G5 clause 2 and G7 clause 1 cannot be unblocked by any card.** They need one
  real project with real data, which arrives with Mihai.
- **G8 and G9** are the only two whose 2026-08-31 blocker is still the true one,
  because it was never the migration: it was the cards. P3-15, P3-16 and
  P3-19 through P3-26 are all still todo and all eligible.

Artefact: `docs/reports/2026-09-08-executor-gate-02-phase3-gate-audit.md`.

**Evidence taken by this run, all re-runnable:**

    GET https://rc-inventory-iota.vercel.app/api/health   200
    {"commit":"49fef9adb89a443076ee2e9879f964c37c823f79","ledger_version":"0036"}

    npm run prove:anon-write-refused    exit 0, 2026-09-08T11:04:56Z
    clients / contacts / suppliers      HTTP 401, 42501, REFUSED, 3 of 3

    npm run check:pending-schema-reads  nothing pending

No migration was added and none was applied.

---

## THE FINDING THIS RUN DID NOT GO LOOKING FOR

**`www.rapidconstructmd.com` no longer serves the inventory application.**

    https://www.rapidconstructmd.com/              301, server: GitHub.com
    https://rapidconstructmd.com/                  200, server: GitHub.com
                                                   last-modified 2026-09-07T21:28:04Z
    https://www.rapidconstructmd.com/api/health    404
    https://www.rapidconstructmd.com/autentificare 404
    https://rc-inventory-iota.vercel.app/          307 -> /autentificare, server: Vercel

The client domain answers from GitHub Pages with a construction company's
marketing site, published yesterday evening. The inventory app is on Vercel.

**Why it matters beyond cosmetics.**
`scripts/poc-free/check-deployed-commit.mjs` defaults to
`https://www.rapidconstructmd.com/api/health`. That check is the one INC-06
produced, and it is what stands between a removal migration and six screens
answering 500. Run with its default today it gets an HTML 404, finds no commit,
and refuses. It fails closed, which is right, but it now refuses on a stale
default rather than on a real risk, and the tempting workaround is `--origin`.

**Reported, not acted on.** GATE-02 is an audit and its defaults forbid building
inside it; repointing a safety guard's default is its own card and its own
decision about which origin is canonical. Logged in `docs/LEARNINGS.md` and in
the audit report under F4.

**A second defect, also reported and not fixed:** P3-12's evidence ref reads
`e2e #PENDING`. Section 6 requires a ref a stranger can re-verify without asking
anyone, and `#PENDING` is not one. It belongs to P3-12, not to this card.

---

## Escalations

None. Nothing on the R-057 list was reached, no card needed a decision its
`defaults` did not cover, and nothing was blocked. Per CLAUDE.md section 4b the
domain finding is a report, not a block, and the run continued past it.

---

## Cap and card count

One card shipped of the two permitted. The second slot was spent inside GATE-02:
a nine-condition clause-by-clause re-derivation is a wide card, and starting a
second one near the cap would have risked leaving work on a branch, which section
13 names as one of the four failure modes a run must distinguish.

---

## What the next run should pick up first

1. **`P3-14`**, which is the next eligible card by the comparator once GATE-02 is
   shipped. Retur de materiale.
2. **Author the four cards GATE-02 recommended and did not write.** In value
   order: the authenticated production verification walk (it alone serves G3, G4,
   G5 clause 1 and G6 clause 1), the anon READ probe (closes G1 clause 2 and
   takes G1 to 4 of 4 for one command), the committed production read of
   `unassigned_outbound_count()`, and repointing `check-deployed-commit`'s
   default origin.
3. **Do not read the phase 3 gate count as a measure of build progress yet.**
   Four of the nine conditions are now blocked on a single missing instrument
   and two more on real client data. Eleven more cards can ship without the
   number moving, exactly as eleven did before this audit.
